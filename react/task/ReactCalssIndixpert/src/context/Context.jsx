import { createContext } from "react";


export const UserContext = createContext()     //user
export const CartContext = createContext()     //Context to managing the cart
export const WishlistContext = createContext() // context to managing the wishlists

